apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: inventory-tls
  namespace: inventory

spec:
  secretName: inventory-tls-secret

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

  dnsNames:
    - yungbunnrith.shop
    - www.yungbunnrith.shop